#!/usr/bin/env node

var path = require('path');
var fs   = require('fs');
var release = path.join(path.dirname(fs.realpathSync(__filename)), '../release');

require(release + '/wordpress-theme-downloader').run();

